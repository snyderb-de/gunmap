{
  "name": "gunmap",
  "version": "0.1.0",
  "description": "using deck.gl to map data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --output-path ./public",
    "start": "webpack-dev-server --conent-base ./public --output-path ./public --hot"
  },
  "author": "Bryan Snyder",
  "license": "ISC",
  "devDependencies": {
    "webpack-cli": "^3.3.10",
    "webpack-dev-server": "^3.9.0"
  }
}
